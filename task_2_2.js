"use strict";

// 2. Чему будет равен x в примере ниже?

var a = 2;
var x = 1 + (a *= 2);

alert(x) // x = 5: a умножается на два с присваиванием результата обратно переменной a (a=4); затем увеличивается на единицу и присваивается переменной x. 
